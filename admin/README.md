# beFoodie Admin (scaffold)

This is a lightweight Vite + React admin scaffold created inside the workspace for prototyping UI.

Notes
- This scaffold is not connected to any backend.
- To run locally:

```bash
cd admin
npm install
npm run dev
```

- The structure is intentionally minimal and ready for further feature additions.
